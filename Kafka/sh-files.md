# **📚 Kafka Tools & Commands - Complete Learning Guide**

## **📌 ক্যাটাগরি ১: কোর অপারেশনস (Core Operations)**

### **🔹 Topic Management**
```
kafka-topics.sh = টপিক তৈরি/ডিলিট/লিস্ট/ডিটেইলস
```
```bash
# Pseudo Code
CREATE TOPIC topic_name 
WITH partitions=3, replication=2

DESCRIBE TOPIC topic_name  # পার্টিশন, লিডার, রেপ্লিকা দেখবে
LIST TOPICS                 # সব টপিকের নাম
DELETE TOPIC topic_name     # টপিক মুছে ফেলবে
```

**কি শেখাবে:**
- টপিক লাইফসাইকেল
- পার্টিশনিং কনসেপ্ট
- রিপ্লিকেশন ফ্যাক্টর
- ব্রোকারে ডেটা ডিস্ট্রিবিউশন

### **🔹 Consumer Groups**
```
kafka-consumer-groups.sh = কনজিউমার গ্রুপ মনিটরিং
```
```bash
# Pseudo Code
SHOW ALL CONSUMER_GROUPS
DESCRIBE GROUP group_name  # ল্যাগ, অফসেট, মেম্বার দেখবে
RESET GROUP OFFSETS        # অফসেট রিসেট
DELETE GROUP group_name    # গ্রুপ ডিলিট
```

**কি শেখাবে:**
- কনজিউমার গ্রুপ মেকানিজম
- অফসেট ম্যানেজমেন্ট
- গ্রুপ রিব্যালান্সিং
- কনজিউমার ল্যাগ

### **🔹 Configuration Management**
```
kafka-configs.sh = ব্রোকার/টপিক কনফিগারেশন
```
```bash
# Pseudo Code
GET CONFIG broker/topic [name]      # কনফিগারেশন পড়বে
SET CONFIG broker/topic [name=value] # কনফিগারেশন সেট করবে
ALTER CONFIG                       # কনফিগারেশন পরিবর্তন
```

**কি শেখাবে:**
- ডাইনামিক কনফিগারেশন
- ব্রোকার/টপিক স্পেসিফিক সেটিংস
- Kafka রেন্টেনশন পলিসি

---

## **📌 ক্যাটাগরি ২: ডেটা অপারেশনস (Data Operations)**

### **🔹 প্রডিউস/কনজিউম ডেটা**
```
kafka-console-producer.sh = কনসোল থেকে ডেটা পাঠানো
kafka-console-consumer.sh = কনসোলে ডেটা পড়া
```
```bash
# Pseudo Code
# Producer
CONNECT TO kafka://localhost:9092
SEND TO topic: "key=value"
SEND TO topic: {"json": "data"}

# Consumer
CONNECT TO kafka://localhost:9092
SUBSCRIBE TOPIC [topic_name]
READ FROM [offset=earliest/latest]
PRINT MESSAGES
```

**কি শেখাবে:**
- প্রডিউসার-কনজিউমার API
- সিরিয়ালাইজেশন/ডিসিরিয়ালাইজেশন
- অফসেট স্ট্র্যাটেজি
- ব্যাচিং ও কম্প্রেশন

### **🔹 পারফরম্যান্স টেস্টিং**
```
kafka-producer-perf-test.sh = প্রডিউসার পারফরম্যান্স
kafka-consumer-perf-test.sh = কনজিউমার পারফরম্যান্স
kafka-e2e-latency.sh = এন্ড-টু-এন্ড লেটেন্সি
```
```bash
# Pseudo Code
TEST PRODUCER PERFORMANCE:
  messages=100000, size=1KB
  MEASURE throughput, latency

TEST CONSUMER PERFORMANCE:
  READ messages, MEASURE speed
  CALCULATE MB/sec, messages/sec

TEST END-TO-END LATENCY:
  SEND timestamped_message
  RECEIVE and CALCULATE delay
```

**কি শেখাবে:**
- Kafka পারফরম্যান্স মেট্রিক্স
- থ্রুপুট অপ্টিমাইজেশন
- লেটেন্সি মেজারমেন্ট
- সিস্টেম লিমিটেশনস

### **🔹 লগ/স্টোরেজ অপারেশনস**
```
kafka-dump-log.sh = লগ ফাইল অ্যানালাইসিস
kafka-log-dirs.sh = স্টোরেজ ডিরেক্টরি চেক
kafka-delete-records.sh = রেকর্ড ডিলিট
```
```bash
# Pseudo Code
DUMP LOG FILE [segment.log]:
  SHOW messages, offsets, timestamps
  FILTER BY timestamp/offset

CHECK STORAGE DIRECTORIES:
  SHOW size, partitions, status
  CHECK disk usage

DELETE RECORDS BEFORE [timestamp]:
  MARK for deletion
  COMPACT segment files
```

**কি শেখাবে:**
- লগ সেগমেন্ট আর্কিটেকচার
- ডিস্ক স্টোরেজ ম্যানেজমেন্ট
- লগ কম্প্যাকশন
- রিটেনশন পলিসি

---

## **📌 ক্যাটাগরি ৩: ক্লাস্টার ম্যানেজমেন্ট (Cluster Management)**

### **🔹 ব্রোকার অপারেশনস**
```
kafka-broker-api-versions.sh = API ভার্সন চেক
kafka-server-start.sh = ব্রোকার স্টার্ট
kafka-server-stop.sh = ব্রোকার স্টপ
```
```bash
# Pseudo Code
START BROKER server.properties
STOP BROKER gracefully
CHECK API COMPATIBILITY between versions
VALIDATE broker configuration
```

**কি শেখাবে:**
- ব্রোকার লাইফসাইকেল
- API ভার্সনিং
- শাটডাউন প্রসিডিউর
- কনফিগারেশন ভ্যালিডেশন

### **🔹 ক্লাস্টার রিপ্লিকেশন**
```
kafka-reassign-partitions.sh = পার্টিশন রিব্যালান্স
kafka-replica-verification.sh = রেপ্লিকা ভেরিফিকেশন
```
```bash
# Pseudo Code
REASSIGN PARTITIONS:
  PLAN reassignment (from_broker → to_broker)
  EXECUTE reassignment
  VERIFY completion
  MONITOR progress

VERIFY REPLICAS:
  COMPARE all replicas
  CHECK consistency
  REPORT discrepancies
```

**কি শেখাবে:**
- ক্লাস্টার রিব্যালান্সিং
- ডেটা মাইগ্রেশন
- রেপ্লিকা কনসিসটেন্সি
- রিকারি প্রসেস

### **🔹 ক্লাস্টার হেলথ**
```
kafka-cluster.sh = ক্লাস্টার স্ট্যাটাস
kafka-get-offsets.sh = অফসেট ইনফো
kafka-metadata-quorum.sh = কুয়োরাম স্ট্যাটাস
```
```bash
# Pseudo Code
CHECK CLUSTER HEALTH:
  GET broker status (up/down)
  GET controller leader
  GET partition leaders
  CHECK quorum status

GET OFFSET INFORMATION:
  SHOW earliest offset
  SHOW latest offset
  CALCULATE lag
```

**কি শেখাবে:**
- ক্লাস্টার মনিটরিং
- কন্ট্রোলার ইলেকশন
- কুয়োরাম মেকানিজম
- লিডার-ফলোয়ার আর্কিটেকচার

---

## **📌 ক্যাটাগরি ৪: সিকিউরিটি & অ্যাডভান্সড (Security & Advanced)**

### **🔹 সিকিউরিটি ম্যানেজমেন্ট**
```
kafka-acls.sh = অ্যাক্সেস কন্ট্রোল লিস্ট
kafka-delegation-tokens.sh = ডেলিগেশন টোকেন
```
```bash
# Pseudo Code
MANAGE ACLs:
  ADD ALLOW user=alice, operation=READ, topic=sales
  ADD DENY user=bob, operation=WRITE, topic=finance
  LIST all ACLs
  REMOVE specific ACL

MANAGE TOKENS:
  CREATE delegation_token for user
  RENEW token validity
  EXPIRE token
```

**কি শেখাবে:**
- Kafka অথেন্টিকেশন
- অথরাইজেশন মডেল
- RBAC (Role-Based Access Control)
- টোকেন-বেইজড সিকিউরিটি

### **🔹 ট্রানজেকশন ম্যানেজমেন্ট**
```
kafka-transactions.sh = ট্রানজেকশন মনিটরিং
```
```bash
# Pseudo Code
LIST ALL TRANSACTIONS:
  SHOW transaction_id, state, timeout
DESCRIBE TRANSACTION tx_id:
  SHOW partitions, producer_id
ABORT TRANSACTION tx_id
```

**কি শেখাবে:**
- ডিস্ট্রিবিউটেড ট্রানজেকশনস
- এটমিসিটি গ্যারান্টি
- ট্রানজেকশনাল স্টেট
- প্রডিউসার আইডেম্পোটেন্সি

### **🔹 কানেক্ট/স্ট্রিমস**
```
connect-distributed.sh = ডিস্ট্রিবিউটেড কানেক্ট
connect-standalone.sh = স্ট্যান্ডালোন কানেক্ট
kafka-streams-*.sh = স্ট্রিমস অ্যাপ্লিকেশন
```
```bash
# Pseudo Code
# Connect
START CONNECT WORKER config.properties
DEPLOY CONNECTOR source/sink
MONITOR connector tasks

# Streams
RESET STREAMS APPLICATION:
  CLEAR local state
  RESET offsets
  RESTART processing
```

**কি শেখাবে:**
- Kafka Connect আর্কিটেকচার
- সোর্স/সিঙ্ক কানেক্টর
- স্ট্রিম প্রসেসিং
- স্টেট ম্যানেজমেন্ট

---

## **📌 ক্যাটাগরি ৫: মেইনটেনেন্স & ট্রাবলশুটিং (Maintenance & Troubleshooting)**

### **🔹 ডায়াগনস্টিক টুলস**
```
kafka-jmx.sh = JMX মেট্রিক্স
kafka-metadata-shell.sh = মেটাডেটা এক্সপ্লোর
kafka-verifiable-*.sh = ভেরিফিকেশন টেস্ট
```
```bash
# Pseudo Code
# JMX Metrics
EXPORT JMX metrics
FILTER BY broker/topic
ANALYZE performance counters

# Metadata Exploration
OPEN metadata shell
NAVIGATE topics/partitions
QUERY specific metadata
```

**কি শেখাবে:**
- পারফরম্যান্স মনিটরিং
- মেটাডেটা ইনস্পেকশন
- সিস্টেম ডায়াগনস্টিকস
- ডিবাগিং টেকনিক

### **🔹 মাইগ্রেশন & আপগ্রেড**
```
zookeeper-security-migration.sh = সিকিউরিটি মাইগ্রেশন
kafka-features.sh = ফিচার ম্যানেজমেন্ট
```
```bash
# Pseudo Code
MIGRATE SECURITY:
  READ old security config
  APPLY new security model
  VALIDATE migration

MANAGE FEATURES:
  ENABLE feature [name]
  DISABLE feature [name]
  CHECK compatibility
```

**কি শেখাবে:**
- ভার্সন মাইগ্রেশন
- ফিচার ফ্ল্যাগস
- ব্যাকওয়ার্ড কম্প্যাটিবিলিটি
- রোলিং আপগ্রেড

---

## **🎯 শেখার স্ট্র্যাটেজি:**

### **Level 1: Beginner (প্রথম সপ্তাহ)**
```
1. kafka-topics.sh → টপিক ম্যানেজমেন্ট
2. kafka-console-producer/consumer.sh → ডেটা ফ্লো
3. kafka-server-start/stop.sh → ব্রোকার বেসিক্স
```

### **Level 2: Intermediate (দ্বিতীয় সপ্তাহ)**
```
1. kafka-consumer-groups.sh → গ্রুপ ম্যানেজমেন্ট
2. kafka-configs.sh → কনফিগারেশন
3. kafka-cluster.sh → ক্লাস্টার হেলথ
4. kafka-get-offsets.sh → অফসেট ম্যানেজমেন্ট
```

### **Level 3: Advanced (তৃতীয় সপ্তাহ)**
```
1. kafka-reassign-partitions.sh → ক্লাস্টার অপারেশনস
2. kafka-acls.sh → সিকিউরিটি
3. kafka-transactions.sh → ট্রানজেকশনস
4. kafka-producer-perf-test.sh → পারফরম্যান্স
```

### **Level 4: Expert (চতুর্থ সপ্তাহ)**
```
1. kafka-dump-log.sh → ইনার ওয়ার্কিংস
2. kafka-metadata-shell.sh → মেটাডেটা
3. connect-distributed.sh → কানেক্ট ফ্রেমওয়ার্ক
4. kafka-streams-*.sh → স্ট্রিম প্রসেসিং
```

---

## **💡 রিয়েল-ওয়ার্ল্ড স্ক্রিপ্ট উদাহরণ:**

### **Daily Health Check Script (Pseudo Code)**
```bash
# ১. ক্লাস্টার স্ট্যাটাস
kafka-cluster.sh cluster-id
kafka-broker-api-versions.sh --bootstrap-server localhost:9092

# ২. টপিক হেলথ
kafka-topics.sh --list | while read topic; do
  kafka-topics.sh --describe --topic $topic
done

# ৩. কনজিউমার ল্যাগ
kafka-consumer-groups.sh --list | while read group; do
  kafka-consumer-groups.sh --describe --group $group
done

# ৪. স্টোরেজ চেক
kafka-log-dirs.sh --describe --bootstrap-server localhost:9092

# ৫. পারফরম্যান্স
kafka-get-offsets.sh --topic important-topic --time -1
```

### **ডিসাস্টার রিকভারি স্ক্রিপ্ট (Pseudo Code)**
```bash
# ব্রোকার ক্র্যাশ হওয়ার পর:
1. CHECK which partitions lost leader
2. REASSIGN leadership to healthy brokers
3. VERIFY data consistency
4. MONITOR replication catch-up
5. ALERT if data loss detected
```

**প্রথমে এগুলো শিখুন:**
1. ✅ `kafka-topics.sh` - টপিক ম্যানেজমেন্ট
2. ✅ `kafka-console-producer/consumer.sh` - ডেটা ফ্লো
3. ✅ `kafka-consumer-groups.sh` - গ্রুপ ম্যানেজমেন্ট
4. ✅ `kafka-configs.sh` - কনফিগারেশন

**প্র্যাকটিস প্রশ্ন:**
1. ৩টি পার্টিশন বিশিষ্ট একটি টপিক তৈরি করুন
2. সেখানে ১০০টি মেসেজ প্রোডিউস করুন
3. দুটি কনজিউমার গ্রুপ দিয়ে পড়ুন
4. প্রতিটি গ্রুপের ল্যাগ চেক করুন
5. একটি পার্টিশন reassign করুন অন্য ব্রোকারে

এখন আপনি প্র্যাকটিস শুরু করতে পারেন! কোন টুল দিয়ে শুরু করবেন? 🚀
